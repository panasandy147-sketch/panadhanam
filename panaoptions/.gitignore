data/*.db
data/*.db-wal
data/*.db-shm
data/*.csv
data/models/
# Whoever is running the desk picked these; they are not the project's.
data/watchlist.json
__pycache__/
*.pyc
.env
# journal/ IS the learning history — it is committed on purpose, not ignored.
journal/*.tmp
